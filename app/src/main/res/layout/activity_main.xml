<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@color/material_dynamic_tertiary10"
        android:elevation="4dp"
        android:fitsSystemWindows="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <Button
        android:id="@+id/add_button_id"
        style="@style/Widget.AppCompat.Button"
        android:layout_width="200dp"
        android:layout_height="72dp"
        android:layout_marginStart="4dp"
        android:text="@string/add_city"
        app:iconTint="@color/material_dynamic_neutral40"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/my_toolbar" />

    <Button
        android:id="@+id/del_button_id"
        style="@style/Widget.AppCompat.Button"
        android:layout_width="200dp"
        android:layout_height="72dp"
        android:layout_marginEnd="4dp"
        android:backgroundTint="#4052B6"
        android:backgroundTintMode="add"
        android:text="@string/delete_city"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toRightOf="@id/add_button_id"
        app:layout_constraintTop_toBottomOf="@id/my_toolbar" />

    <ListView
        android:id="@+id/city_list"
        android:layout_width="match_parent"
        android:layout_height="700dp"
        android:background="#DFB9F4"
        app:layout_constraintTop_toBottomOf="@+id/del_button_id" />

    <LinearLayout
        android:id="@+id/add_box"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="#F4F0F4"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <EditText
            android:id="@+id/city"
            android:layout_width="195dp"
            android:layout_height="57dp"
            android:layout_marginStart="18dp"
            android:autofillHints="Edmonton"
            android:hint="@string/enter_a_city"
            android:inputType="text"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium" />

        <Button
            android:id="@+id/confirm_button_id"
            style="@style/Widget.AppCompat.Button"
            android:layout_width="150dp"
            android:layout_height="68dp"
            android:layout_marginStart="32dp"
            android:text="@string/confirm" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>